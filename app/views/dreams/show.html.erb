<%= @dream.content %>
<div id="js-meanings">
  <% unless current_user %>
    <%= link_to "Add Meaning", new_user_session_path %>
  <% else %>
    <%= link_to "Add Meaning", [:new, @dream, :meaning], id: "js-new-meaning-link", remote: true %>
  <% end %>
  <%= render @meanings %>
</div
